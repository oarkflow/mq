{
	"type": "object",
	"description": "users",
	"required": [ "user_id" ],
	"properties": {
		"last_name": {
			"type": "string",
			"default": "now()"
		},
		"user_id": {
			"type": [
				"integer",
				"string"
			],
			"maxLength": 64
		}
	}
}